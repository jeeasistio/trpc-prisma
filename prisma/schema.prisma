generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model countries {
  id         String @id @db.Uuid
  name       String
  region     String
  continent  String
  capital    String
  population Int
}
